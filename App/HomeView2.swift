//
//  HomeView.swift
//  App
//
//  Created by German Zvezdin on 26.02.2021.
//

import SwiftUI
import UIKit
/*
struct HomeView2: View {
    @AppStorage("pin_status") var pinned = false
    @AppStorage("username") var username = ""
    @AppStorage("password") var password = ""
    @AppStorage("X-Subject-Token") var Token = ""
    @ObservedObject var MetaData: MetricsData = MetricsData()
    init() {
        MetaData.LoadVms(token: Token)
        
    }
    var body: some View {
        VStack {
            Button(action: {
                print(MetaData.VmList)
                print(MetaData.MetricsStorage)
            }, label: {
                Text("Button")
            })
            
            Button(action: {
                pinned = false
                Token = ""
                username = ""
                password = ""
            }, label: {
                Text("logout")
            })
        } .onAppear {
            UserDefaults.standard.set(false, forKey: "first_log")
        }
    }
}

struct HomeView_Previews: PreviewProvider {
    static var previews: some View {
        HomeView2()
    }
}
*/
